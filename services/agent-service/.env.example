# Service configuration
SERVICE_NAME=agent-service
PORT=8000
APP_ENV=development
LOG_LEVEL=INFO
CORE_DATABASE_NAME=call_e_core
LLM_PROVIDER=mock
GROQ_API_KEY=
GROQ_MODEL=
MAX_TOOL_ITERATIONS=5

# Register the Kaari tenant + agent on startup so real phone calls can resolve it.
AGENT_SERVICE_SEED=true
