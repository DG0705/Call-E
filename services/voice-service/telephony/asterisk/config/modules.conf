; Development-only module list for a minimal Call-E Asterisk instance.
; Load the PJSIP channel and the ARI module the voice adapter targets.
; ARI (res_ari) is required by the HTTP transport foundation.

[modules]
autoload = yes
noload = chan_sip.so
load = res_pjsip.so
load = res_ari.so
