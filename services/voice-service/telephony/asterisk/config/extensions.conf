; Development-only dialplan placeholder for the Call-E voice service adapter.
; In production, route outbound channels to your SIP trunk and secure the
; from-internal context. No numbers or credentials are committed here.

[from-internal]
exten => _X.,1,NoOp(Call-E outbound channel started)

[call-e-inbound]
exten => s,1,NoOp(Call-E inbound channel accepted)
