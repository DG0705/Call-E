; Development-only RTP configuration for the Call-E voice service adapter.
; Constrain the RTP port range so the Docker host mapping stays small.
; The voice service's RTP ingress listener targets this same range.

[general]
rtpstart = 10000
rtpend = 10099
icesupport = no
stunaddr =
